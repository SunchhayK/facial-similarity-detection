# Facial Similarity Detection

This project is a facial similarity detection system that uses deep learning to detect similar faces in images. It uses a convolutional neural network (CNN) to extract features from faces and then compares them using cosine similarity.

## Pre-requisites

- Python 3.11+
- uv
